# Сюда бот записывает тех, кто ему написал, но в списке получателей не значится.
#
# Нужно, чтобы подключить нового человека: его chat_id больше неоткуда взять —
# Оповещения.log не попадает в репозиторий, а логи GitHub Actions без авторизации
# не читаются.
#
# Формат:  дата  chat_id  имя
#
# Чтобы подключить человека — допиши его chat_id через запятую в секрет
# WB_TELEGRAM_CHAT. Строку отсюда после этого можно удалить, она больше не нужна.
#
# Файл существует всегда, даже пустой: git add по отсутствующему файлу уронил бы
# шаг сохранения, потому что GitHub выполняет run-блоки как bash -e.
